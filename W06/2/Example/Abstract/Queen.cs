class Queen : ChessPiece {
    public Queen(int x, int y, char color) : base(x, y, color) {

    }

    //We need to use override here so that we can use this function
    public override void Move(int destX, int destY) {

    }
}