//We create a class called DNA
public class DNA {
    //In the DNA object we have a variable called seq
    public string seq;

    //In its constructer we set the seq value of the Object to the inputted sequence
    public DNA(string str_seq) {
        seq = str_seq;
    }
}